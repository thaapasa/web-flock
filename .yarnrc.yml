nodeLinker: node-modules

# No dependency in this project declares a preinstall/install/postinstall
# script, so nothing is lost by refusing to run them — and that is the vector
# the July 2025 eslint-config-prettier compromise used. If a future dependency
# genuinely needs to build on install, allow it by name here rather than
# turning this back on wholesale.
enableScripts: false
